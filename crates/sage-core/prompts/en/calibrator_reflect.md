The following are {count} errors that were corrected by the user in historical {report_type} reports:
{corrections_text}

Please analyze: what common pattern do these errors share? What is the root cause?
Provide 1-2 specific self-constraint rules so that future {report_type} reports do not repeat these kinds of errors.
Each rule on its own line, starting with 'Rule:', no more than 50 characters.